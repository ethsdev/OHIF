diff --git a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/AngleTool.js b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/AngleTool.js
index 6c8b361..f98fd1a 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/AngleTool.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/AngleTool.js
@@ -473,6 +473,7 @@ class AngleTool extends AnnotationTool {
 }
 function defaultGetTextLines(data, targetId) {
     const cachedVolumeStats = data.cachedStats[targetId];
+    const { label } = data;
     const { angle } = cachedVolumeStats;
     if (angle === undefined) {
         return;
@@ -480,9 +481,14 @@ function defaultGetTextLines(data, targetId) {
     if (isNaN(angle)) {
         return [`${angle}`];
     }
-    const textLines = [
-        `${csUtils.roundNumber(angle)} ${String.fromCharCode(176)}`,
-    ];
+    const textLines = [];
+
+    if (label) {
+        textLines.push(label);
+    }
+
+    textLines.push(`${csUtils.roundNumber(angle)} ${String.fromCharCode(176)}`);
+
     return textLines;
 }
 export default AngleTool;
diff --git a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/CircleROITool.js b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/CircleROITool.js
index 4458d7b..8f8a440 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/CircleROITool.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/CircleROITool.js
@@ -620,8 +620,12 @@ class CircleROITool extends AnnotationTool {
 }
 function defaultGetTextLines(data, targetId) {
     const cachedVolumeStats = data.cachedStats[targetId];
+    const { label } = data;
     const { radius, radiusUnit, area, mean, stdDev, max, isEmptyArea, areaUnit, modalityUnit, } = cachedVolumeStats;
     const textLines = [];
+    if (label) {
+        textLines.push(label);
+    }
     if (radius) {
         const radiusLine = isEmptyArea
             ? `Radius: Oblique not supported`
diff --git a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/EllipticalROITool.js b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/EllipticalROITool.js
index f385199..9ebed58 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/EllipticalROITool.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/EllipticalROITool.js
@@ -695,12 +695,19 @@ class EllipticalROITool extends AnnotationTool {
 }
 function defaultGetTextLines(data, targetId) {
     const cachedVolumeStats = data.cachedStats[targetId];
+    const { label } = data;
     const { area, mean, stdDev, max, isEmptyArea, areaUnit, modalityUnit } = cachedVolumeStats;
     const textLines = [];
+
+    if (label) {
+        textLines.push(label);
+    }
+
     if (area) {
         const areaLine = isEmptyArea
             ? `Area: Oblique not supported`
             : `Area: ${csUtils.roundNumber(area)} ${areaUnit}`;
+
         textLines.push(areaLine);
     }
     if (mean) {
diff --git a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/PlanarFreehandROITool.js b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/PlanarFreehandROITool.js
index f282148..d79f8c6 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/PlanarFreehandROITool.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/PlanarFreehandROITool.js
@@ -505,8 +505,12 @@ class PlanarFreehandROITool extends ContourSegmentationBaseTool {
 }
 function defaultGetTextLines(data, targetId) {
     const cachedVolumeStats = data.cachedStats[targetId];
+    const { label } = data;
     const { area, mean, stdDev, length, perimeter, max, isEmptyArea, unit, areaUnit, modalityUnit, } = cachedVolumeStats || {};
     const textLines = [];
+    if (label) {
+        textLines.push(label);
+    }
     if (area) {
         const areaLine = isEmptyArea
             ? `Area: Oblique not supported`
diff --git a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RectangleROITool.js b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RectangleROITool.js
index b9f4528..3cfe9b8 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RectangleROITool.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RectangleROITool.js
@@ -559,11 +559,15 @@ class RectangleROITool extends AnnotationTool {
 }
 function defaultGetTextLines(data, targetId) {
     const cachedVolumeStats = data.cachedStats[targetId];
+    const { label } = data;
     const { area, mean, max, stdDev, areaUnit, modalityUnit } = cachedVolumeStats;
     if (mean === undefined || mean === null) {
         return;
     }
     const textLines = [];
+    if (label) {
+        textLines.push(label);
+    }
     textLines.push(`Area: ${csUtils.roundNumber(area)} ${areaUnit}`);
     textLines.push(`Mean: ${csUtils.roundNumber(mean)} ${modalityUnit}`);
     textLines.push(`Max: ${csUtils.roundNumber(max)} ${modalityUnit}`);
diff --git a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/SplineROITool.js b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/SplineROITool.js
index 427d3ca..166ec24 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/SplineROITool.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/SplineROITool.js
@@ -770,8 +770,12 @@ class SplineROITool extends ContourSegmentationBaseTool {
 }
 function defaultGetTextLines(data, targetId) {
     const cachedVolumeStats = data.cachedStats[targetId];
+    const { label } = data;
     const { area, isEmptyArea, areaUnit } = cachedVolumeStats;
     const textLines = [];
+    if (label) {
+        textLines.push(label);
+    }
     if (area) {
         const areaLine = isEmptyArea
             ? `Area: Oblique not supported`
diff --git a/node_modules/@cornerstonejs/tools/dist/esm/utilities/getCalibratedUnits.js b/node_modules/@cornerstonejs/tools/dist/esm/utilities/getCalibratedUnits.js
index 37761b8..d93cf71 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/utilities/getCalibratedUnits.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/utilities/getCalibratedUnits.js
@@ -86,10 +86,16 @@ const getCalibratedLengthUnitsAndScale = (image, handles) => {
         const physicalDeltaY = Math.abs(region.physicalDeltaY);
         const isSamePhysicalDelta = utilities.isEqual(physicalDeltaX, physicalDeltaY, EPS);
         if (isSamePhysicalDelta) {
-            scale = 1 / physicalDeltaX;
-            calibrationType = 'US Region';
-            unit = UNIT_MAPPING[region.physicalUnitsXDirection] || 'unknown';
-            areaUnit = unit + SQUARE;
+            // COMMENTED OUT: US region scaling - use default scale for consistency
+            // scale = 1 / physicalDeltaX;
+            // calibrationType = 'US Region';
+            // unit = UNIT_MAPPING[region.physicalUnitsXDirection] || 'unknown';
+            // areaUnit = unit + SQUARE;
+
+            // Use default scale (same as outside region) for consistency
+            scale = 1; // Keep default scale
+            calibrationType = ''; // No "US Region" suffix
+            // unit and areaUnit keep their default values from lines 36-38
         }
         else {
             return {
